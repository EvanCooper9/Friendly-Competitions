{
  "scripts": {
    "build-functions": "cd functions && npm run build",
    "serve": "npm run build-functions && firebase emulators:start --import=exported-dev-data --export-on-exit=exported-dev-data"
  }
}
