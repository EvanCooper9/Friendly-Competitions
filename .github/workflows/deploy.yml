name: Deploy to AppStore Connect

on:
  release:
    types: [published]

jobs:
  build:
    name: Deploy
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Homebrew
        run: brew bundle
      - name: Setup GoogleService-Info.plist
        run: |
          GOOGLE_SERVICE_INFO="Friendly Competitions/Firebase/Release/GoogleService-Info.plist"
          touch $GOOGLE_SERVICE_INFO
          echo $GOOGLE_SERVICE_INFO_DEBUG > GOOGLE_SERVICE_INFO
        env:
          GOOGLE_SERVICE_INFO_RELEASE: ${{ secrets.GOOGLE_SERVICE_INFO_RELEASE }}
      - name: Deploy
        run: fastlane release
        env:
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD }}
          FASTLANE_USER: ${{ secrets.FASTLANE_USER }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}